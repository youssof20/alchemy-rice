# Gallery entries

This directory contains metadata-only gallery submissions. Each JSON file must describe one
creator-owned, immutable rice release and pass the validator documented in
[`GALLERY.md`](../../GALLERY.md).

Do not add rice archives, screenshots, binaries, scripts, or copied repository content here.
